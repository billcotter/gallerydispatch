---
description: Apply when inspecting Figma designs via MCP for The Gallery Dispatch. Use together with design-integration.mdc.
---

# Figma MCP Integration

Use this rule together with `design-integration.mdc` whenever a task involves
inspecting Figma via MCP for The Gallery Dispatch.

`design-integration.mdc` governs authority, architecture, accessibility,
semantics, CSS, JavaScript, SEO, validation, and scope.

This file covers only the Figma MCP inspection workflow.

## Required inspection flow

1. Run `get_design_context` first for the exact Figma node(s) being reviewed or
   implemented.

2. If the design-context response is too large, incomplete, or truncated:
   - run `get_metadata` to inspect the node hierarchy;
   - identify the smallest relevant nodes;
   - call `get_design_context` again for only those nodes.

3. Run `get_screenshot` for the exact frame/component/variant being implemented.

4. When variables or design tokens matter, inspect them with
   `get_variable_defs` rather than copying raw visual values from the screenshot.

5. Inspect existing repository components, CSS, tokens, and architecture before
   proposing code changes.

6. Translate Figma visual intent through `design-integration.mdc`.

Only begin implementation after both structured Figma context and visual
screenshot/context have been reviewed.
